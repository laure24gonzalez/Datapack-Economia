# Aplica la transferencia real al jugador objetivo.
# Este archivo está pensado para un uso futuro con un selector fijo.
# Por ahora, el jugador ejecutor se usa como remitente y el objetivo se define desde el sistema de entrada.

tellraw @s {"text":"⚠️ La transferencia directa requiere un selector objetivo definido por el sistema de entrada.","color":"yellow"}
